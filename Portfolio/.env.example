# Copy to .env (already gitignored) and fill in.
# Get a free Basic key from https://api.henrikdev.xyz/dashboard/ -> API Keys -> Generate New Key.
# The key is only read server-side (Vite dev/preview proxy and the Cloudflare Pages
# function under functions/api/henrik) and is never shipped in the browser bundle.
HENRIKDEV_API_KEY=
